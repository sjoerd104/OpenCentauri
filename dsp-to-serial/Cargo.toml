[package]
name = "dsp-to-serial"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0", features = ["fs", "ioctl", "poll"] }
memmap2 = "0"
thiserror = "2"
serialport = "4"

[profile.release]
strip = true
lto = true